'use client'; import {useState} from 'react'; export default function Login(){const [view,setView]=useState<'start'|'customer'|'admin'>('start');return <main className="login"><a className="back" href="/">← Soul Lifting Therapies</a><section>{view==='start'?<><p className="eyebrow">Your private space</p><h1>Welcome back.</h1><p>Choose how you would like to continue.</p><button className="login-choice" onClick={()=>setView('customer')}><b>I’m a customer</b><span>Appointments, progress and rewards</span></button><button className="login-choice" onClick={()=>setView('admin')}><b>I’m Emma</b><span>Manage treatments, availability and bookings</span></button></>:<><button className="text-button" onClick={()=>setView('start')}>← Back</button><p className="eyebrow">{view==='admin'?'Emma’s admin area':'Customer account'}</p><h1>{view==='admin'?'Manage your business.':'Your wellbeing, in one place.'}</h1><p>This is a safe preview. When live, we’ll send a secure one-time sign-in code by email.</p><label>Email address<input placeholder="you@example.com" type="email"/></label><button className="primary">Send test sign-in code</button>{view==='admin'?<a className="secondary" href="/portal">View admin preview</a>:<a className="secondary" href="/account">View customer preview</a>}</>}</section></main>}
